# Configuración de producción para Oracle Database
app.database.enabled=true

# Configuración de Oracle Database  
spring.datasource.url=jdbc:oracle:thin:@ctapiad_high
spring.datasource.username=HUERTO
spring.datasource.password=Dws2024**
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA / Hibernate properties
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect
spring.jpa.properties.hibernate.format_sql=true

# Configuración del servidor
server.port=8081

# Configuración CORS
cors.allowed.origins=*

# Configuración de logging
logging.level.com.fullstack.usuario=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Thymeleaf
spring.thymeleaf.check-template-location=false